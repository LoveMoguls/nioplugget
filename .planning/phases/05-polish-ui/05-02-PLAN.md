---
phase: 05-polish-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/+layout.svelte
  - frontend/src/routes/dashboard/+page.svelte
  - frontend/src/routes/study/+page.svelte
  - frontend/src/routes/study/[subject]/+page.svelte
  - frontend/src/routes/study/[subject]/[topic]/+page.svelte
  - frontend/src/routes/chat/[sessionId]/+page.svelte
  - frontend/src/routes/login/+page.svelte
  - frontend/src/routes/register/+page.svelte
  - frontend/src/routes/child/login/+page.svelte
  - frontend/src/routes/invite/[token]/+page.svelte
  - frontend/src/app.html
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - "All views render without horizontal scroll at 320px viewport width"
    - "Hamburger menu appears below md breakpoint (768px) and horizontal nav appears above it"
    - "All interactive elements have minimum 44px touch target"
    - "Notched devices have safe area padding"
  artifacts:
    - frontend/src/routes/+layout.svelte
    - frontend/src/app.html
  key_links:
    - "+layout.svelte controls navigation for all authenticated pages"
---

<objective>
Make all views mobile-ready with hamburger navigation, touch-friendly targets, and safe area support.

Purpose: Ensure the app is fully usable on mobile phones (iOS Safari and Android Chrome) — the primary device for students.
Output: Responsive hamburger menu in layout, touch-optimized tap targets across all pages, safe area CSS for notched devices.
</objective>

<execution_context>
@/home/admin/.claude/get-shit-done/workflows/execute-plan.md
@/home/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish-ui/05-CONTEXT.md

@frontend/src/routes/+layout.svelte
@frontend/src/routes/dashboard/+page.svelte
@frontend/src/routes/study/+page.svelte
@frontend/src/routes/chat/[sessionId]/+page.svelte
@frontend/src/app.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add viewport meta and safe area support</name>
  <files>frontend/src/app.html</files>
  <action>
Ensure app.html has the correct viewport meta tag with viewport-fit=cover for notched devices:

```html
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
```

If it already has a viewport meta tag, update it. If not, add it in `<head>`.

This enables `env(safe-area-inset-*)` CSS values to work on iOS Safari for notched iPhones.
  </action>
  <verify>
    <automated>grep -c "viewport-fit=cover" /home/admin/projekt/nioplugget/frontend/src/app.html</automated>
  </verify>
  <done>app.html has viewport meta with viewport-fit=cover</done>
</task>

<task type="auto">
  <name>Task 2: Implement hamburger menu and mobile navigation</name>
  <files>frontend/src/routes/+layout.svelte</files>
  <action>
Replace the current simple nav bar with a responsive hamburger menu:

**Desktop (md: and above):**
- Keep current horizontal layout: logo left, navigation links right
- Show all nav links inline (Dashboard for parents, Plugga for children, Logga ut)

**Mobile (below md:):**
- Logo left, hamburger icon (Lucide `Menu` icon) right
- Tapping hamburger opens an overlay panel (full-width, slides down or appears below nav bar)
- Overlay contains nav links stacked vertically with generous padding (min 44px height per link)
- Tapping a link closes the menu and navigates
- Tapping hamburger again (now showing Lucide `X` icon) closes the menu
- Add `aria-expanded` attribute to the hamburger button
- Add `aria-label="Meny"` to the hamburger button

**Implementation:**
- Use `$state(false)` for menu open state
- Import `Menu` and `X` from `@lucide/svelte` (already in devDependencies)
- Use Tailwind classes: `hidden md:flex` for desktop links, `md:hidden` for hamburger button
- Menu overlay: absolute or fixed positioning below the nav bar, full width, bg-card with border-b
- Add safe area padding: `padding-left: env(safe-area-inset-left)`, `padding-right: env(safe-area-inset-right)` on the nav container
- Close menu on navigation by adding `onclick` handler that sets menuOpen = false

**Navigation links to include:**
- For parents ($isParent): Dashboard (/dashboard)
- For children (!$isParent): Plugga (/study)
- For all logged-in users: Logga ut (button)

Keep the existing `onMount(async () => { await user.checkAuth(); })` and the conditional `{#if $isLoggedIn}` logic unchanged. The hamburger menu only applies inside the logged-in nav.

Add bottom safe area padding to the `<main>` element: `padding-bottom: env(safe-area-inset-bottom, 0px)`.
  </action>
  <verify>
    <automated>cd /home/admin/projekt/nioplugget/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>Hamburger menu visible on mobile viewports, horizontal nav on desktop; menu toggles open/closed; all links have min 44px touch target</done>
</task>

<task type="auto">
  <name>Task 3: Mobile-optimize all page layouts</name>
  <files>
    frontend/src/routes/dashboard/+page.svelte
    frontend/src/routes/study/+page.svelte
    frontend/src/routes/study/[subject]/+page.svelte
    frontend/src/routes/study/[subject]/[topic]/+page.svelte
    frontend/src/routes/chat/[sessionId]/+page.svelte
    frontend/src/routes/login/+page.svelte
    frontend/src/routes/register/+page.svelte
    frontend/src/routes/child/login/+page.svelte
    frontend/src/routes/invite/[token]/+page.svelte
  </files>
  <action>
Audit and fix each page for mobile responsiveness. Key changes:

**All pages:**
- Ensure no fixed widths that could cause horizontal scroll at 320px
- Buttons must have min-h of 44px (Tailwind: `min-h-[44px]`) — check all Button components
- Links used as navigation must have adequate padding for 44px touch target

**Dashboard (/dashboard):**
- Already uses `max-w-4xl px-4` — should work. Verify forms and buttons are full-width on mobile
- API key masked display: ensure `flex` layout doesn't overflow on small screens — use `flex-wrap` or stack vertically on mobile

**Study pages (/study, /study/[subject], /study/[subject]/[topic]):**
- Grid columns: ensure `grid-cols-1` base with `sm:grid-cols-2` or `lg:grid-cols-3` (study/+page.svelte already does this for subjects)
- Review cards: verify they don't overflow

**Chat page (/chat/[sessionId]):**
- This is the most critical mobile layout
- Message area should be scrollable with fixed input at the bottom
- Input bar should respect safe area bottom inset: `padding-bottom: env(safe-area-inset-bottom, 0px)`
- Chat bubbles should have max-width (e.g., `max-w-[85%]`) so they don't span full width
- Text input and send button should be comfortable to use on mobile

**Auth pages (login, register, child/login, invite/[token]):**
- Already centered cards with `max-w-md` — likely fine
- Ensure input fields are large enough for mobile (already using shadcn Input which should be fine)
- Verify forms don't overflow on 320px viewport

Do NOT change any logic or functionality. CSS/layout changes only.
  </action>
  <verify>
    <automated>cd /home/admin/projekt/nioplugget/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>All pages render correctly at 320px width with no horizontal scroll; all interactive elements have 44px minimum touch target; chat input respects safe area inset</done>
</task>

</tasks>

<verification>
- Build succeeds: `cd frontend && npm run build`
- No TypeScript errors from svelte-check
- Hamburger menu icon visible in layout at mobile viewport
- All pages use responsive Tailwind classes (no fixed pixel widths > 320px)
</verification>

<success_criteria>
- All views usable on mobile phone without horizontal scroll or broken layouts
- Hamburger menu for mobile navigation (not bottom tab bar, per CONTEXT decision)
- Touch-friendly tap targets (min 44px) on all interactive elements
- Safe area handling for notched devices
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-ui/05-02-SUMMARY.md`
</output>
