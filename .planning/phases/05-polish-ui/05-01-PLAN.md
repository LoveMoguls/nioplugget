---
phase: 05-polish-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app.css
  - frontend/src/routes/+layout.svelte
autonomous: true
requirements:
  - UI-02
must_haves:
  truths:
    - "Color palette uses calm blue/green tones in both light and dark mode"
    - "Dark mode activates automatically based on system prefers-color-scheme"
    - "No gamification elements (badges, streaks, trophies) exist anywhere in the UI"
  artifacts:
    - frontend/src/app.css
    - frontend/src/routes/+layout.svelte
  key_links:
    - "CSS custom properties in app.css drive all component colors via shadcn tokens"
---

<objective>
Apply calm blue/green visual identity and automatic dark mode detection.

Purpose: Replace the default achromatic shadcn palette with a calm, studious color scheme that reflects the educational nature of the product. Parents are the audience — the design should feel trustworthy and focused.
Output: Updated app.css with new color palette, +layout.svelte with system dark mode detection.
</objective>

<execution_context>
@/home/admin/.claude/get-shit-done/workflows/execute-plan.md
@/home/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish-ui/05-CONTEXT.md

@frontend/src/app.css
@frontend/src/routes/+layout.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update color palette to calm blue/green tones</name>
  <files>frontend/src/app.css</files>
  <action>
Replace the existing achromatic color palette in app.css with calm blue/green tones using oklch format (already in use).

**Light mode palette guidance:**
- `--color-background`: near-white with very subtle warm tint (e.g., `oklch(0.99 0.002 210)`)
- `--color-foreground`: deep blue-gray (e.g., `oklch(0.20 0.02 240)`)
- `--color-primary`: muted teal/blue-green for buttons and links (e.g., `oklch(0.45 0.12 195)`)
- `--color-primary-foreground`: white/near-white for text on primary
- `--color-secondary`: very light blue-green tint for secondary surfaces
- `--color-muted`: light gray with subtle blue undertone
- `--color-muted-foreground`: medium gray-blue for secondary text
- `--color-accent`: soft sage/green for hover states and accents
- `--color-accent-foreground`: dark text for readability on accent
- `--color-destructive`: keep existing red (works across themes)
- `--color-border`: light gray with subtle blue tint
- `--color-input`: same as border
- `--color-ring`: medium blue-green for focus rings
- `--color-card`: white or near-white
- `--color-card-foreground`: same as foreground

**Dark mode palette (in `.dark` selector):**
- Mirror the light palette structure but with appropriate dark variants
- Background: deep blue-gray (e.g., `oklch(0.17 0.02 240)`)
- Foreground: light cream/white
- Primary: slightly brighter teal that works on dark backgrounds
- Maintain sufficient contrast ratios (WCAG AA: 4.5:1 for text)

Keep the existing `@theme`, `@custom-variant dark`, `@layer base` structure.
Keep `--radius-*` values unchanged.
Keep `--font-sans` unchanged (system fonts per CONTEXT discretion).

Do NOT add animations or transitions. Do NOT add gamification colors (gold, orange streak colors, etc.).
  </action>
  <verify>
    <automated>cd /home/admin/projekt/nioplugget/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>app.css has calm blue/green color palette in both light and dark mode variants, no achromatic-only colors remain for primary/accent/ring tokens</done>
</task>

<task type="auto">
  <name>Task 2: Add automatic dark mode detection</name>
  <files>frontend/src/routes/+layout.svelte</files>
  <action>
Add system dark mode detection in the +layout.svelte `onMount`:

1. Check `window.matchMedia('(prefers-color-scheme: dark)')` on mount
2. If matches, add `'dark'` class to `document.documentElement`; otherwise remove it
3. Listen for changes with `matchMedia.addEventListener('change', callback)` to react to system theme switches while the app is open
4. Clean up the listener in an onMount return function (Svelte 5 pattern: return cleanup from onMount)

No manual toggle — per REQUIREMENTS out-of-scope list, dark/light toggle is v2. System preference only.

Keep the existing nav/main structure unchanged.
  </action>
  <verify>
    <automated>cd /home/admin/projekt/nioplugget/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>Opening the app in a browser with dark system preference shows dark mode; switching system preference changes the theme without reload</done>
</task>

</tasks>

<verification>
- Build succeeds: `cd frontend && npm run build`
- No TypeScript errors from svelte-check
- app.css contains oklch values with non-zero chroma for primary, accent, and ring colors
- +layout.svelte has matchMedia dark mode detection
</verification>

<success_criteria>
- Color palette uses calm blue/green tones (not achromatic gray)
- Both light and dark mode palettes defined
- Dark mode activates automatically from system preference
- No gamification aesthetics present
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-ui/05-01-SUMMARY.md`
</output>
