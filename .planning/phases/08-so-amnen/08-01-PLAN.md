---
phase: 08-so-amnen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/db/migrations/007_seed_so_amnen.up.sql
  - backend/db/migrations/007_seed_so_amnen.down.sql
autonomous: true
requirements:
  - GEO-01
  - GEO-02
  - GEO-03
  - GEO-04
  - HIST-01
  - HIST-02
  - HIST-03
  - HIST-04

must_haves:
  truths:
    - "Migration 007 up.sql inserts Geografi (display_order=6) and Historia (display_order=7) into subjects"
    - "All 8 topic slugs are URL-safe transliterations (å→a, ä→a, ö→o, space→-)"
    - "Geografi has exactly 4 topics in display_order 1-4"
    - "Historia has exactly 4 topics in display_order 1-4"
    - "Migration 007 down.sql deletes both subjects by slug (CASCADE removes topics+exercises)"
    - "up.sql contains exactly 24 SYSTEM_PROMPT_PLACEHOLDER entries — one per exercise"
  artifacts:
    - path: "backend/db/migrations/007_seed_so_amnen.up.sql"
      provides: "Subject and topic inserts for Geografi and Historia — scaffold with exercise INSERT placeholders"
      contains: "INSERT INTO subjects.*geografi.*historia"
    - path: "backend/db/migrations/007_seed_so_amnen.down.sql"
      provides: "Rollback — deletes Geografi and Historia subjects (CASCADE removes topics and exercises)"
      contains: "DELETE FROM subjects WHERE slug IN"
  key_links:
    - from: "backend/db/migrations/007_seed_so_amnen.up.sql"
      to: "subjects table"
      via: "INSERT with display_order=6 (Geografi), display_order=7 (Historia)"
      pattern: "INSERT INTO subjects"
    - from: "backend/db/migrations/007_seed_so_amnen.up.sql"
      to: "topics table"
      via: "CROSS JOIN VALUES pattern keyed by subject slug"
      pattern: "WHERE s.slug = 'geografi'"
---

<objective>
Create the migration scaffold for Phase 8 — write both SQL migration files with all subject and topic inserts for Geografi and Historia, plus exercise INSERT stubs (with SYSTEM_PROMPT_PLACEHOLDER) to be completed in plans 02 and 03.

Purpose: Establish the correct migration structure, subject display order, topic slugs, and down migration before plan 02 and 03 fill in the exercise system prompts. Correct subjects and topics in place allow plans 02 and 03 to focus entirely on prompt content quality.

Output: `007_seed_so_amnen.up.sql` (subjects + topics + exercise INSERT stubs for 8 topics), `007_seed_so_amnen.down.sql` (CASCADE delete)
</objective>

<execution_context>
@/home/admin/.claude/get-shit-done/workflows/execute-plan.md
@/home/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-so-amnen/08-RESEARCH.md

<interfaces>
<!-- Key patterns from migration 006 (Phase 7 — direct reference). Executor must follow these exactly. -->

From backend/db/migrations/006_seed_no_amnen.up.sql (subject insert pattern):
```sql
INSERT INTO subjects (name, slug, display_order) VALUES
    ('Kemi', 'kemi', 4),
    ('Fysik', 'fysik', 5);
```

From backend/db/migrations/006_seed_no_amnen.up.sql (topic insert pattern):
```sql
INSERT INTO topics (subject_id, name, slug, display_order)
SELECT s.id, t.name, t.slug, t.display_order
FROM subjects s
CROSS JOIN (VALUES
    ('Materiens uppbyggnad', 'materiens-uppbyggnad', 1),
    ('Kemiska reaktioner', 'kemiska-reaktioner', 2),
    ('Kemikalier i vardagen', 'kemikalier-i-vardagen', 3),
    ('Kemi och hållbar utveckling', 'kemi-och-hallbar-utveckling', 4)
) AS t(name, slug, display_order)
WHERE s.slug = 'kemi';
```

From backend/db/migrations/006_seed_no_amnen.up.sql (exercise stub pattern):
```sql
INSERT INTO exercises (topic_id, title, description, difficulty_order, system_prompt)
SELECT t.id, e.title, e.description, e.difficulty_order, e.system_prompt
FROM topics t
JOIN subjects s ON t.subject_id = s.id
CROSS JOIN (VALUES
    ('Exercise title', 'Short Swedish description.', 1, 'SYSTEM_PROMPT_PLACEHOLDER'),
    ('Exercise title', 'Short Swedish description.', 2, 'SYSTEM_PROMPT_PLACEHOLDER'),
    ('Exercise title', 'Short Swedish description.', 3, 'SYSTEM_PROMPT_PLACEHOLDER')
) AS e(title, description, difficulty_order, system_prompt)
WHERE s.slug = 'kemi'
  AND t.slug = 'materiens-uppbyggnad';
```

From backend/db/migrations/006_seed_no_amnen.down.sql (rollback pattern):
```sql
DELETE FROM subjects WHERE slug IN ('kemi', 'fysik');
-- ON DELETE CASCADE removes all topics and exercises automatically
```

Existing display_order values in subjects table after Phase 7:
- 1 = Biologi
- 2 = Samhällskunskap
- 3 = Matematik
- 4 = Kemi
- 5 = Fysik
- 6 = Geografi (Phase 8)
- 7 = Historia (Phase 8)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write down migration</name>
  <files>backend/db/migrations/007_seed_so_amnen.down.sql</files>
  <action>
Create `backend/db/migrations/007_seed_so_amnen.down.sql` with a single DELETE statement:

```sql
-- Rollback: remove Geografi and Historia subjects and all associated topics and exercises
-- ON DELETE CASCADE in schema handles topics and exercises automatically
DELETE FROM subjects WHERE slug IN ('geografi', 'historia');
```

This is the complete down migration — no other content needed.
  </action>
  <verify>
    <automated>cat /home/admin/projekt/nioplugget/backend/db/migrations/007_seed_so_amnen.down.sql</automated>
  </verify>
  <done>File exists, contains DELETE statement with both slugs ('geografi', 'historia')</done>
</task>

<task type="auto">
  <name>Task 2: Write up migration scaffold — subjects, topics, exercise stubs</name>
  <files>backend/db/migrations/007_seed_so_amnen.up.sql</files>
  <action>
Create `backend/db/migrations/007_seed_so_amnen.up.sql` following the exact same structure as migration 006. Use `'SYSTEM_PROMPT_PLACEHOLDER'` for all system_prompt values — plans 02 and 03 will replace them.

**Section 1 — Subjects:**
```sql
-- Insert Geografi and Historia subjects
-- display_order continues from Phase 7: Biologi=1, Samhällskunskap=2, Matematik=3, Kemi=4, Fysik=5
INSERT INTO subjects (name, slug, display_order) VALUES
    ('Geografi', 'geografi', 6),
    ('Historia', 'historia', 7);
```

**Section 2 — Geografi topics (4 topics):**
```sql
-- Geografi topics
INSERT INTO topics (subject_id, name, slug, display_order)
SELECT s.id, t.name, t.slug, t.display_order
FROM subjects s
CROSS JOIN (VALUES
    ('Befolkning och urbanisering', 'befolkning-och-urbanisering', 1),
    ('Klimat och klimatförändringar', 'klimat-och-klimatforandringar', 2),
    ('Naturresurser och hållbarhet', 'naturresurser-och-hallbarhet', 3),
    ('Geopolitik och handel', 'geopolitik-och-handel', 4)
) AS t(name, slug, display_order)
WHERE s.slug = 'geografi';
```

**Section 3 — Historia topics (4 topics):**
```sql
-- Historia topics
INSERT INTO topics (subject_id, name, slug, display_order)
SELECT s.id, t.name, t.slug, t.display_order
FROM subjects s
CROSS JOIN (VALUES
    ('Industrialismens tid', 'industrialismens-tid', 1),
    ('De två världskrigen', 'de-tva-varldskrigen', 2),
    ('Kalla kriget', 'kalla-kriget', 3),
    ('1900-talets politiska rörelser', '1900-talets-politiska-rorelser', 4)
) AS t(name, slug, display_order)
WHERE s.slug = 'historia';
```

**Section 4 — Exercise INSERT stubs for all 8 topics:**

Follow the exact CROSS JOIN VALUES pattern from migration 006. Use the exercise titles, descriptions, and slugs listed below. Use `'SYSTEM_PROMPT_PLACEHOLDER'` for system_prompt in all 24 rows.

**Geografi exercise stubs:**

befolkning-och-urbanisering:
- ('Befolkningsfördelning och migration', 'Förstå push/pull-faktorer och befolkningstäthet.', 1, 'SYSTEM_PROMPT_PLACEHOLDER')
- ('Urbanisering och globalisering', 'Förstå vad som driver urbanisering och hur globalisering påverkar migration.', 2, 'SYSTEM_PROMPT_PLACEHOLDER')
- ('Befolkningstillväxt och resurser', 'Resonera kring sambandet mellan befolkningstillväxt, resurser och hållbarhet.', 3, 'SYSTEM_PROMPT_PLACEHOLDER')

klimat-och-klimatforandringar:
- ('Klimatzoner och klimatfaktorer', 'Förstå jordens klimatzoner och skillnaden mellan klimat och väder.', 1, 'SYSTEM_PROMPT_PLACEHOLDER')
- ('Global uppvärmning och konsekvenser', 'Förklara sambandet mellan växthusgaser och global uppvärmning.', 2, 'SYSTEM_PROMPT_PLACEHOLDER')
- ('Klimatåtgärder och ansvarsfördelning', 'Resonera kring länders ansvar för klimatåtgärder och Parisavtalet.', 3, 'SYSTEM_PROMPT_PLACEHOLDER')

naturresurser-och-hallbarhet:
- ('Förnybara och icke-förnybara resurser', 'Förstå skillnaden mellan fossila bränslen och förnybara energikällor.', 1, 'SYSTEM_PROMPT_PLACEHOLDER')
- ('Vattentillgång och energiresurser', 'Förklara varför vattenbrist uppstår och hur energiresurser påverkar geopolitik.', 2, 'SYSTEM_PROMPT_PLACEHOLDER')
- ('Hållbar resurshushållning', 'Resonera kring hållbar utveckling och SDG-målen.', 3, 'SYSTEM_PROMPT_PLACEHOLDER')

geopolitik-och-handel:
- ('Världshandel och ekonomiska system', 'Förstå vad BNP mäter och vad handelsmönster innebär.', 1, 'SYSTEM_PROMPT_PLACEHOLDER')
- ('Globala organisationer och beroenden', 'Förklara hur FN och WTO påverkar världshandeln.', 2, 'SYSTEM_PROMPT_PLACEHOLDER')
- ('Geopolitiska konflikter och Nord-Syd', 'Resonera kring orsaker till ekonomiska klyftor mellan Nord och Syd.', 3, 'SYSTEM_PROMPT_PLACEHOLDER')

**Historia exercise stubs:**

industrialismens-tid:
- ('Industrialiseringens orsaker och spridning', 'Förstå vad industrialisering innebar och vilka uppfinningar som var centrala.', 1, 'SYSTEM_PROMPT_PLACEHOLDER')
- ('Sociala rörelser och arbetsvillkor', 'Förklara varför fackföreningar bildades och hur arbetsvillkoren förändrades.', 2, 'SYSTEM_PROMPT_PLACEHOLDER')
- ('Industrialismens konsekvenser', 'Resonera kring industrialismens långsiktiga konsekvenser för samhälle och miljö.', 3, 'SYSTEM_PROMPT_PLACEHOLDER')

de-tva-varldskrigen:
- ('Första världskrigets orsaker', 'Förstå de viktigaste orsakerna till första världskriget och allianssystemet.', 1, 'SYSTEM_PROMPT_PLACEHOLDER')
- ('Andra världskriget och Förintelsen', 'Förklara hur Versaillesfreden bidrog till andra världskrigets utbrott och hur Förintelsen var möjlig.', 2, 'SYSTEM_PROMPT_PLACEHOLDER')
- ('Världskrigens efterdyningar', 'Resonera kring hur de två världskrigen omformade den internationella ordningen.', 3, 'SYSTEM_PROMPT_PLACEHOLDER')

kalla-kriget:
- ('Den bipolära världsordningen', 'Förstå vad kalla kriget innebar och vad bipolär världsordning betyder.', 1, 'SYSTEM_PROMPT_PLACEHOLDER')
- ('Proxykonflikt och kapprustning', 'Förklara vad proxykonflikt innebär och hur kapprustningen påverkade säkerhetsläget.', 2, 'SYSTEM_PROMPT_PLACEHOLDER')
- ('Kalla krigets slut och dekolonisation', 'Resonera kring hur kalla kriget påverkade dekolonisationsprocessen.', 3, 'SYSTEM_PROMPT_PLACEHOLDER')

1900-talets-politiska-rorelser:
- ('Fascism och kommunism som ideologier', 'Förstå grunddragen i fascism och kommunism och vad totalitär stat innebär.', 1, 'SYSTEM_PROMPT_PLACEHOLDER')
- ('Demokratisering och mänskliga rättigheter', 'Förklara hur FNs deklaration om mänskliga rättigheter kom till och vad som drev demokratiseringsrörelser.', 2, 'SYSTEM_PROMPT_PLACEHOLDER')
- ('Politiska rörelsers arv', 'Resonera kring hur 1900-talets politiska rörelser formar vår samtid.', 3, 'SYSTEM_PROMPT_PLACEHOLDER')

**Slug anti-pattern warning:** Verify all slugs are URL-safe. Correct slugs:
- 'klimat-och-klimatforandringar' (not 'klimatförändringar')
- 'naturresurser-och-hallbarhet' (not 'hållbarhet')
- 'de-tva-varldskrigen' (not 'två' or 'världskrigen')
- '1900-talets-politiska-rorelser' (not 'rörelser')
- 'industrialismens-tid', 'kalla-kriget', 'geopolitik-och-handel', 'befolkning-och-urbanisering' (no special chars — safe as-is)

**No apostrophes in descriptions** — descriptions above are clean.
  </action>
  <verify>
    <automated>grep -c "SYSTEM_PROMPT_PLACEHOLDER" /home/admin/projekt/nioplugget/backend/db/migrations/007_seed_so_amnen.up.sql</automated>
  </verify>
  <done>File exists with exactly 24 SYSTEM_PROMPT_PLACEHOLDER entries, 2 subject inserts, 8 topic inserts, all slugs URL-safe</done>
</task>

</tasks>

<verification>
After both tasks:
- `007_seed_so_amnen.down.sql` exists with DELETE statement for 'geografi' and 'historia'
- `007_seed_so_amnen.up.sql` exists with INSERT for 2 subjects, 8 topics, 24 exercise stubs
- `grep -c SYSTEM_PROMPT_PLACEHOLDER backend/db/migrations/007_seed_so_amnen.up.sql` returns exactly 24
- All slugs are URL-safe (no å, ä, ö in slug values)
- File is syntactically valid SQL (no unbalanced parentheses, quotes, or missing semicolons)
</verification>

<success_criteria>
Migration scaffold committed with correct structure — plans 02 and 03 can proceed to fill in NP-calibrated system prompts without any structural changes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-so-amnen/08-01-SUMMARY.md`
</output>
