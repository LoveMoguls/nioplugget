---
feature: laxutmaningar
task: 12
total_tasks: 12
status: complete
last_updated: 2026-07-04T00:00:00.000Z
---

<current_state>
Läxutmaningar is feature-complete. All 12 tasks done (some superseded by a
better flow than originally planned).
</current_state>

<completed_work>

- Tasks 1-7: backend (migrations 008-010, sqlc queries, generate/store/handler,
  chat challenge sessions, routes, frontend API client) — committed earlier
- Task 8: StarAnimation.svelte created. UploadModal superseded by the dashboard
  inline upload + draft/publish flow (migration 009: challenges.published)
- Task 9: /challenges list page superseded — children see published challenges
  on /study, parents manage them on /dashboard
- Task 10: /challenges/[id] page with star display, sequential unlocking,
  progress line, star animation on ?stars=&xp= return
- Task 11: study + dashboard entry points done
- Task 12: chat page honors ?returnTo= (validated, internal paths only) and
  redirects to /challenges/[id]?stars=N&xp=N after ending a challenge session

Also applied: Fable 5 code review fixes (ownership/published authz, progress
view includes challenge sessions, cookie helper unification, CORS PATCH,
SM-2 context fix, dedupe, index 010) — see commit d435af3.
</completed_work>

<remaining_work>

- None for this feature. Optional future ideas: unpublish/rename without
  publish, background job for the ~45s generation call, shared httputil
  package for the per-package uuid/writeJSON helpers.
</remaining_work>

<context>
Golden-path manual test (not yet run end-to-end with a real API key):
parent uploads photos on /dashboard → publishes → child opens /study →
challenge → Starta → chat → Avsluta pass → redirected with star animation →
next exercise unlocked.
</context>
